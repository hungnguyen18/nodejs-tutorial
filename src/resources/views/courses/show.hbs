<div class="mt-4">
    <div class="row">
        <div class="col-lg-3">
            <button class="btn btn-primary">Đăng ký</button>
        </div>
        <div class="col-lg-9">
            <h1>{{course.name}}</h1>
            <img src="{{course.img}}" alt="" srcset="">
            <p>{{course.description}}</p>
        </div>
    </div>
</div>